group = "com.elouyi"
version = Versions.ywBot

allprojects {
    repositories {
        maven("https://maven.aliyun.com/repository/public/")
        mavenCentral()
        dependencies{
            //implementation(project(":src"))

        }
    }
}
